{
  "name": "sat-practice",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Digital SAT mock test. The UI in public/ ships as plain ES modules; npm is only needed to run its tests.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
